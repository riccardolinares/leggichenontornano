/*
 * Tailwind v4 non ha più un file di configurazione JavaScript: il tema si
 * dichiara nel CSS, accanto ai colori che il progetto aveva già. Qui resta
 * solo il collegamento a PostCSS.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
