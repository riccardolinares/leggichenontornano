node_modules/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo
.env
.env.local
.DS_Store
/var/
/.cache/
/tmp/
playwright-report/
test-results/
# Il corpus grezzo (centinaia di MB di XML) si riscarica con `pnpm fetch`.
/data/corpus/

# Gli archivi della Corte costituzionale (decine di MB di zip) si riscaricano
# con `antinomia-corpus consulta`.
/data/consulta/

# Il dataset derivato, invece, sta nel repository: serve a far girare il sito e
# i test da un clone appena fatto, ed e' il dataset che chiunque puo' verificare.
.vercel
